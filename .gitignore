# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env
.env.prod
deploy/Caddyfile.active

# Frontend
frontend/node_modules/
frontend/dist/

# Test / harness output
*_pytest_*.txt
harness_run_all.txt

# Offline bundle build output + real offline env (the .example stays tracked)
deploy/offline/dist/
tess-offline-bundle-*.tar.gz
tess-offline-bundle-*.tar.gz.sha256
.env.offline
