# P2 cluster mesh inventory (docs/P2_OPENER.md §(a) as-built fleet).
# No secrets live here: WG private keys are generated on-node and never leave
# their node; public keys are recorded in the as-built record after keygen.
# The observer (5.161.245.44) is deliberately absent — it is not on the private
# network or the mesh (opener §(b)).
MESH_NODES="node1 node2 node3"

NODE1_PUBLIC_IP=78.47.69.162
NODE1_CLOUD_IP=10.0.0.2
NODE1_WG_IP=10.8.0.1

NODE2_PUBLIC_IP=5.75.186.164
NODE2_CLOUD_IP=10.0.0.3
NODE2_WG_IP=10.8.0.2

NODE3_PUBLIC_IP=77.42.71.248
NODE3_CLOUD_IP=10.0.0.4
NODE3_WG_IP=10.8.0.3

WG_IFACE=wg0
WG_PORT=51820
